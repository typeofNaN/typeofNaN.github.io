declare namespace Api {
  namespace MapPointApi {
    interface Detail {
      mapPointId: number; title?: string; longitude: number; latitude: number
      occurredTime: string; mediaUrl?: string
    }
  }
}
